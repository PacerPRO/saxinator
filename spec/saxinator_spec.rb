require 'spec_helper'

describe Saxinator do
  it 'has a version number' do
    expect(Saxinator::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
